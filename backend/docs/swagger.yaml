basePath: /api
definitions:
  dto.ErrorResponse:
    properties:
      message:
        type: string
      result:
        type: string
    type: object
  dto.GetMembersByBirthdayMonthResponse:
    properties:
      members:
        items:
          $ref: '#/definitions/dto.MemberBirthday'
        type: array
      result:
        type: string
    type: object
  dto.MemberBirthday:
    properties:
      date:
        type: integer
      month:
        type: integer
      name:
        type: string
    type: object
info:
  contact: {}
  description: 235botのデータを取得・操作したり、認証処理をするためのAPI
  license:
    name: Maki
  title: 235Bot API
  version: "1.0"
paths:
  /discord/auth:
    get:
      produces:
      - application/json
      responses:
        "302":
          description: Found
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/dto.ErrorResponse'
      summary: Discord OAuth2 認証API
      tags:
      - discord
  /discord/auth/callback:
    get:
      parameters:
      - description: Discord OAuth2認証コード
        in: query
        name: code
        type: string
      - description: OAuth2 state
        in: query
        name: state
        type: string
      - description: Discord OAuth2認証エラー
        in: query
        name: error
        type: string
      produces:
      - application/json
      responses:
        "302":
          description: Found
      summary: Discord OAuth2 認証コールバックAPI
      tags:
      - discord
  /members:
    get:
      consumes:
      - application/json
      parameters:
      - description: Birthday month (1-12)
        in: query
        name: birthday_month
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/dto.GetMembersByBirthdayMonthResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/dto.ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/dto.ErrorResponse'
      summary: 指定された月が誕生日のメンバー一覧を取得するAPI
      tags:
      - members
swagger: "2.0"
